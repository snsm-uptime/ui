/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {}, // Load TailwindCSS
    autoprefixer: {}, // Recommended for vendor prefixing
  },
};

export default config;
